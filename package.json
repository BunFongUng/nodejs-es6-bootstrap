{
  "name": "todos-api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build":
      "npm run clean && mkdir -p dist && babel src -D -d dist --source-maps inline",
    "clean": "rm -rf dist",
    "dev": "nodemon --exec babel-node src/server.js"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "nodemon": "^1.17.2"
  },
  "dependencies": {
    "body-parser": "^1.18.2",
    "compression": "^1.7.2",
    "dotenv": "^5.0.1",
    "express": "^4.16.3",
    "express-validator": "^5.1.0",
    "mongoose": "^5.0.12",
    "morgan": "^1.9.0"
  }
}
